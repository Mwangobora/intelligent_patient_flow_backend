# UI Demo Seed Data

This folder contains local-only demo data for UI testing.

Run from the backend repository root:

```bash
docker compose exec -T api python manage.py dbshell < database/seeds/ui_demo_data.sql
docker compose exec -T api python manage.py backfill_codes
```

Demo logins:

```text
Staff/web email: demo.user01@patientflow.local
Patient mobile email: demo.user12@patientflow.local
Password: DemoPass123!
```

Seed scope:

- Organizations: 5
- Facility types: 4
- Facilities: 10
- Departments: 30
- Specialties: 10
- Facility specialties: 30
- Service point types: 4
- Service points: 30
- Consultation rooms: 30
- Operating hours: 70
- Schedule exceptions: 10
- Users: 30
- Permissions: dynamic backend permission rows for the current UI modules
- Roles: 16
- Patients: 60
- Patient identifiers: 50
- Patient addresses: 50
- Related persons and contacts: 30 each
- Practitioner types: 5
- Practitioners: 30
- Practitioner assignments: 30 facility, 30 department, 30 specialty
- Practitioner credentials: 30
- Availability periods: 30
- Practitioner shifts: 30
- Appointment slots: 60
- Appointments: 50
- Appointment status history: initial plus current-state rows
- Check-ins: 40
- Check-in tokens: 20
- Queues: 10
- Queue entries: 40
- Queue entry events: joined/called/skipped/service events
- Intelligence predictions: 50
- Notifications: 50
- Push devices: 30
- Report exports: 30
- Audit logs: 60

Security notes:

- Passwords use a Django-compatible PBKDF2 hash. The demo password is only for local development.
- Sensitive encrypted fields use synthetic `enc:ui-demo-*` placeholders, not plaintext patient secrets.
- Hash fields use valid SHA-256 hex values generated by PostgreSQL `pgcrypto`.
- Raw QR/check-in tokens and raw push tokens are not stored.
- The file does not disable foreign keys, checks, exclusion constraints, or append-only triggers.
- The seed sets database timestamp defaults where the migrated local database relies on Django application-level defaults, matching the project SQL schema for direct SQL imports.
- Generated-code columns are still present in this direct SQL seed because those columns are NOT NULL and direct SQL bypasses Django services. Normal API/UI creates generate codes in the backend and do not accept user-entered codes.
